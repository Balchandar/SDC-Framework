# Deployment

Target: linux-x86_64

The service ships as a container image for Linux on x86_64. The image exposes
a health check and reads its configuration from the environment. The initial
deployment target is Docker on Linux.
